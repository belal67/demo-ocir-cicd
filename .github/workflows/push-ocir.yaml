name: pipeline
on:
  push

jobs:
  publish:
    runs-on: ubuntu
    steps:
      - uses: actions/checkout@v3
      - name: build
        run: |
          docker login --username belal67 --password ${{ PASSWORD.password }}  ghcr.io
          docker pull ghcr.io/belal67/hello

